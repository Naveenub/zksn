# ZKSN i2pd tunnel configuration — Docker dev environment
# Defines internal services accessible only from within I2P.

# Example: ZKSN internal web service
# [zksn-web]
# type = http
# host = 127.0.0.1
# port = 8080
# keys = zksn-web.dat
# inPort = 80
