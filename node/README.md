# zksn-node

Mix node for the Zero-Knowledge Sovereign Network.

## Run

```bash
cp node.toml.example node.toml
cargo run --package zksn-node -- --config node.toml --testnet
```

## Config: `node.toml`

See `node.toml.example` — all fields documented inline.
